# Гімназія №4 - Система Управління Харчуванням

Повнофункціональна система для управління харчуванням учнів з підтримкою хмарного зберігання через Cloudflare Workers + KV.

## 🚀 Функціонал

### Три ролі користувачів:

1. **Класний керівник** - подання щоденних заявок на харчування
2. **Працівник їдальні** - перегляд заявок, генерація звітів (денні/місячні)
3. **Адміністратор** - управління класами, PIN-кодами, доступ до архіву

### Основні можливості:

- ✅ Рольова система з PIN-аутентифікацією
- ✅ Подання заявок вчителями (кількість учнів, харчування вчителя)
- ✅ Перегляд у реальному часі (картки класів)
- ✅ Денні та місячні звіти з можливістю друку
- ✅ Експорт у CSV (UTF-8 BOM для Excel)
- ✅ Фільтрація за змінами (1-ша, 2-га, всі)
- ✅ Хмарна синхронізація через Cloudflare Workers + KV
- ✅ Offline режим (LocalStorage як резервне сховище)
- ✅ Автооновлення кожні 2 хвилини
- ✅ Адаптивний дизайн (мобільні, планшети, десктоп)

## 📁 Структура проекту

```
gym4-nutrition/
├── index.html          # Основний HTML
├── styles.css          # Всі стилі
├── app.js              # Вся логіка додатку
├── worker.js           # Cloudflare Worker (бекенд)
└── README.md           # Ця інструкція
```

## 🔧 Налаштування

### 1. Розгортання на GitHub Pages

1. Створіть репозиторій на GitHub
2. Завантажте файли: `index.html`, `styles.css`, `app.js`
3. Налаштування → Pages → Source: main branch
4. Ваш сайт буде доступний за адресою: `https://username.github.io/repo-name`

### 2. Налаштування Firebase (бекенд)

**Швидка інструкція:**

1. [console.firebase.google.com](https://console.firebase.google.com) → Create Project
2. Realtime Database → Create Database (Europe, Test mode)
3. Project Settings → Web app → Copy config
4. Оновіть `FIREBASE_CONFIG` в `app.js`
5. Database → Rules → встановіть правила доступу

**Детальна інструкція:** `FIREBASE_SETUP.md`

### 3. Налаштування правил Firebase

```json
{
  "rules": {
    "classes": { ".read": true, ".write": true },
    "reports": { ".read": true, ".write": true },
    "config": { ".read": true, ".write": true }
  }
}
```

## 🔐 PIN-коди за замовчуванням

- **Адміністратор**: `1312`
- **Їдальня**: `5555`
- **Класи** (в `app.js`):
  - 1-А: `1111` (Мельник О.М.)
  - 2-Б: `2222` (Коваль С.І.)
  - 5-В: `5555` (Бондар Ю.В.)
  - 6-А: `6666` (Шевченко Т.Г.)
  - 9-Б: `9999` (Франко І.Я.)

**Важливо**: Змініть PIN-коди після першого запуску!

## 📱 Використання

### Для Вчителя:
1. Оберіть роль "Класний керівник"
2. Введіть PIN свого класу
3. Заповніть форму (всього учнів, фактично присутні, будуть їсти)
4. Вкажіть, чи будете харчуватися ви
5. Натисніть "Відправити заявку"

### Для Їдальні:
1. Оберіть роль "Працівник їдальні"
2. Введіть PIN (за замовчуванням: 5555)
3. Перегляд:
   - **Картки** - візуальний моніторинг заявок
   - **День** - таблиця для друку з підписами
   - **Місяць** - матриця на весь місяць
4. Експорт у CSV або Друк

### Для Адміністратора:
1. Оберіть роль "Адміністратор"
2. Введіть PIN (за замовчуванням: 1312)
3. Вкладки:
   - **Класи** - додавання/редагування/видалення
   - **Звіти** - як у їдальні
   - **Архів** - всі подані заявки
   - **Безпека** - зміна PIN їдальні

## 🎨 Кастомізація

### Зміна кольорів

У файлі `styles.css` в секції `:root` можна змінити кольори:

```css
:root {
    --indigo-600: #4f46e5;  /* Основний колір */
    --emerald-600: #059669; /* Акцентний колір */
    /* ... інші змінні */
}
```

### Додавання класів

В `app.js` знайдіть `INITIAL_CLASSES` та додайте:

```javascript
{ 
    id: '3a', 
    className: '3-А', 
    shift: 1, 
    teacherName: 'Іваненко І.І.', 
    pin: '3333' 
}
```

## 🔄 Синхронізація даних

Система працює в двох режимах:

1. **Online** - дані синхронізуються з Cloudflare KV
2. **Offline** - дані зберігаються локально в LocalStorage

При поверненні онлайн дані автоматично синхронізуються.

## 📊 Експорт даних

CSV файли створюються з:
- UTF-8 BOM (для коректного відкриття в Excel)
- Роздільник: `;` (працює з українською локалізацією Excel)
- Формати: денний звіт або місячна матриця

## 🖨️ Друк

При натисканні "Друк":
- Приховуються всі кнопки та меню
- Залишається тільки таблиця
- Автоматичне форматування під A4
- Підписи для адміністрації та їдальні

## ⚡ Оптимізація

- Автооновлення кожні 2 хв (можна змінити в `CONFIG.AUTO_REFRESH_INTERVAL`)
- Кешування в LocalStorage
- Мінімальний розмір файлів (без залежностей)
- Швидке завантаження (~50KB загальний розмір)

## 🐛 Усунення неполадок

### Worker не працює:
1. Перевірте, чи прив'язано KV namespace
2. Перевірте URL worker в `app.js`
3. Перевірте CORS налаштування

### Дані не зберігаються:
1. Перевірте консоль браузера (F12)
2. Переконайтеся, що LocalStorage увімкнено
3. Перевірте розмір даних (ліміт ~10MB)

### PIN не працює:
1. Перевірте правильність введення (4 цифри)
2. Перевірте налаштування в адмін-панелі
3. Очистіть LocalStorage і перезавантажте

## 📝 Ліцензія

Код надається як є, для використання Гімназією №4. Можна адаптувати для інших закладів.

## 🤝 Підтримка

При виникненні проблем:
1. Перевірте консоль браузера
2. Перевірте налаштування Cloudflare Worker
3. Очистіть кеш браузера

## 📈 Майбутні покращення

- [ ] Статистика харчування за тиждень/місяць
- [ ] Експорт у Excel (XLSX)
- [ ] Push-сповіщення
- [ ] Мобільний додаток (PWA)
- [ ] Інтеграція з Google Sheets
- [ ] Автоматичні нагадування вчителям
